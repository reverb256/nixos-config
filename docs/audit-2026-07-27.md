# Multi-Area Audit Report — 2026-07-27

> **Last Verified:** 2026-07-27 (re-verified and corrected 2026-07-27 post first draft — see §Auto-fixes applied, item 2)
> **Auditor:** Buffy (Claude Sonnet 4.6 via Freebuff; bug-hunter skill + thinker-with-files-gemini pass)
> **Scope:** All five audit dimensions — Security & secrets, NixOS declarative hygiene, code quality / dead code, cluster / architecture, documentation drift
> **Branch:** main (run `git log -1 --oneline` from /etc/nixos for the current HEAD)
> **Coverage:** ~268 `.nix` files, ~268 `kubernetes-manifests/*`, ~78 `.md`. Static analysis + signals + cross-check vs filesystem. `nix flake check` NOT run locally (Zephyr 31 GB RAM → OOM risk per knowledge.md); `nix-instantiate --parse` ran on every modified `.nix` file (SYNTAX_OK).

---

## TL;DR

- **5 HIGH/MEDIUM findings** that need **operator action** (cannot be auto-fixed safely):
  1. `.env` is **currently tracked by git** and contains `SAMSUNG_TV_TOKEN="27212631"` (real-looking TV token committed to repo)
  2. `SAMSUNG_TV_TOKEN` route in `secretspec.toml` points at `secrets/default/activepieces.yaml` (the typo'd-path file from 2026-07-25 incident) — operator must rotate and route to a properly-named file
  3. **YubiKey CCID driver symlink missing on zephyr** — both YubiKey recipients in `.sops.yaml` are registered but unused; decryption falls through to plaintext keys. SECURITY-INCIDENT-2026-07-25.md flagged this; recipe inside.
  4. **4 live Kubernetes manifests use `:latest` image tags** despite `deny-latest-tag.yaml` policy
  5. `kubernetes-manifests/knowledge-fabric/01-deployment.yaml` has 4 hardcoded host IPs (breaks on restart per knowledge.md)

- **6 LOW-RISK fixes applied via auto-edit** (committed in this branch): see "Auto-fixes applied" below.

- **Documentation rot** is the largest single category of defects — 4 audit-related docs (INFRASTRUCTURE-AUDIT.md, STATUS.md, RUNBOOK.md, knowledge.md sections) declare themselves stale per the Pocock Rule.

---

## Prior audits (cross-reference)

| Date | Audit | Key outcomes still relevant today |
|------|-------|-----------------------------------|
| 2026-03-21 | `DOCUMENTATION_AUDIT_2026-03-21.md` | 90% file reduction (754→78 .md); subsequent audits must not reproduce that mistake |
| 2026-05-14 | `INFRASTRUCTURE-AUDIT.md` | 71-day-old point-in-time snapshot. **All numeric figures in this file are stale**. Replaced header with ARCHIVED — DO NOT FOLLOW warning in this audit |
| 2026-07-25 | `SECURITY-INCIDENT-2026-07-25.md` | SOPS data-loss recovery; 17/18 secrets recovered from git; **`SAMSUNG_TV_TOKEN` placeholder still at `secrets/default/activepieces.yaml`**, YubiKey CCID symlink still missing on zephyr; `.sops.yaml.bak-2026-07-25` cleanup still pending |
| 2026-07-25 | sops-nix → secretspec Phase 2 migration | **COMPLETE**. secretspec-resolved secrets flow through NDJSON over stdio per cachix/secretspec#98. Dual-path runs: `Path A` (secretspec) live, `Path B` (sops-nix) retained for backwards compatibility, Phase 3 = remove Path B |

---

## Auto-fixes applied (this branch)

All auto-fixes were reviewed by code-reviewer-minimax-m3. Two flagged NITs re-verified as non-issues (`.env.*` does NOT match `env.secrets.example` — no leading dot; `overlay.nix` self-reference-cycle breadcrumb at lines 5–9 is intact). Files remain untracked, so a `git add -A && git diff --cached` is required to inspect.

### 1. `.gitignore` — added `.env`, `.env.*`, `.secretspec.env`

**Reason:** `.env` (containing `SAMSUNG_TV_TOKEN="27212631"`) is currently tracked by git. Adding to `.gitignore` prevents future commits. **Operator action still required:** `git rm --cached .env` to un-track the existing file (cannot auto-fix: user must confirm the file is not needed in git history).

### 2. `secretspec.toml` — corrected `route_count` 24 → 49

**Reason:** `grep -c 'ref = { item'` returns **49** sops-routed secrets across `[profiles.default]` + `[profiles.production]` + `[profiles.development]`. Manifest self-doc was 71% stale (#24 off-by-25, presumably the count before the Nexus-added ref block landed).

### 3. `overlay.nix` — removed dead commented-out llama-cpp-dflash / dflash-server / ai-inference-gateway block

**Reason:** Block was commented out with a `TODO: broken placeholder rev/hash` comment and provided no executable function. The header breadcrumb at lines 5–9 (self-reference-cycle explanation for `caddy-with-modules`) is intact.

### 4. `hosts/zephyr/secretspec-creds-wiring.nix` — removed dead `ZAI_API_KEY` entry

**Reason:** `INFRASTRUCTURE-AUDIT.md` confirms Z.AI was removed 2026-07-15 (MCP servers, LLM provider, secrets). `modules/services/hermes-cli.nix:145` documents the removal. Replacing the dead wiring with an explanatory comment prevents future `git grep` confusion while the consumer side (`modules/development/ai-coding-tools.nix`, `k8s/opencode/deployment.yaml`) finishes its own Z.AI cleanup (tracked as **Finding F-12**).

### 5. `INFRASTRUCTURE-AUDIT.md` — replaced `⚠️ STALE` notice with `⚠️⚠️⚠️ ARCHIVED — DO NOT FOLLOW`

**Reason:** Original framing implied temporary staleness; at 71+ days, every numeric figure is unusable. Explicit ARCHIVED header + drift markers list (post-2026-05-14 changes) makes the state-of-the-file unambiguous to future maintainers.

### 6. `docs/LIVE/STATUS.md` and `docs/LIVE/RUNBOOK.md` — added prominent STALE warnings at top

**Reason:** Last-verified 2026-05-24 (>2 months). Per AGENTS.md Pocock Rule, must be prominently flagged so they're not followed blindly.

---

## Findings by area (HIGH → LOW priority)

### 🔴 SECURITY & SECRETS

#### F-1. **`.env` is in the git index** (staged-but-not-committed) and contains real-looking `SAMSUNG_TV_TOKEN="27212631"` ⚠️ HIGH

**Evidence:**
- `git ls-files --error-unmatch .env` succeeds → file is in the git index (i.e. has been `git add`-ed at least once).
- `git log --all --full-history -- .env` returns **0 commits** → file has NEVER been committed in repo history, but is staged at the current index level. This means a future `git commit` (current or recent) could promote it into history with no additional warning.
- `.gitignore` did not cover `.env` directly (template `.env.secrets` was covered; bare `.env` was not). This audit's auto-fix #1 added forward-going coverage. **The existing tracked file is not untracked by a gitignore edit alone.**

**Impact:** A real-looking TV WebSocket control token (`27212631`) is in git history. If the token is genuine and still valid, anyone with git clone access can control the TV. If the token is fake / expired, the risk is only operational (drift from `.env.secrets` template that says `replace-me-locally` for the same key).

**Operator action:**
1. Confirm with the operator whether `27212631` is a genuine TV token and, if so, rotate it at the TV admin UI and re-encrypt the value into `secrets/default/samsung-tv-token.yaml` (replacing the typo'd-path placeholder).
2. `git rm --cached .env` to un-track without deleting the local file (the on-disk value is needed for SDK operation).
3. Verify `.env.secrets.example` template still ships unchanged.

#### F-2. **`SAMSUNG_TV_TOKEN` route is broken** — points at `default/activepieces.yaml` (the typo'd placeholder path from 2026-07-25 incident) ⚠️ HIGH

**Evidence:**
- `secretspec.toml:211`: `SAMSUNG_TV_TOKEN = { ... ref = { item = "default/activepieces.yaml#data" } }` — noted as a typo by SECURITY-INCIDENT-2026-07-25.md
- SECRETSPEC-validator passes because dotenv fallback kicks in (`.env` resolves `SAMSUNG_TV_TOKEN="27212631"`, hiding the broken sops route)
- `secrets/default/activepieces.yaml` exists but contains placeholder data

**Impact:** Validator's `✓` for `SAMSUNG_TV_TOKEN` is misleading — it's reflecting dotenv resolution, NOT a successful sops decryption. Cluster silently relies on a stale value.

**Operator action:**
1. Add a dedicated `secrets/default/samsung-tv-token.yaml` (sops-encrypted).
2. Update `secretspec.toml:211` route to `secrets/default/samsung-tv-token.yaml#data` (and the host wiring for whoever needs it).
3. Update `sops updatekeys -y` to align recipients.
4. Delete the typo'd placeholder at `secrets/default/activepieces.yaml` (or rename it to `samsung-tv-token.yaml` and re-encrypt with the real value).

#### F-3. **YubiKey CCID driver symlink missing on zephyr** ⚠️ MEDIUM (operator-verification-required on zephyr host; this audit ran from local files only)

**Evidence:** `SECURITY-INCIDENT-2026-07-25.md` (Security Insight number 2) — `pcscd` runs on zephyr but hotplug layer cannot find CCID driver bundle at hardcoded `/var/lib/pcsc/drivers` path. Two YubiKey recipients in `.sops.yaml` are registered-but-unused; decryption falls through to `cluster_age` or `zephyr_age_v2` (both plaintext files). `knowledge.md` "YubiKey/pcscd" section confirms the recipe.

**Note:** This audit ran from the repository tree and could not SSH into zephyr to verify the live symlink state. The recipe below must be confirmed-applied on zephyr host.

**Impact:** Non-blocking today (SOPS OR-semantics rescue). **Blocking if zephyr loses both software keys** — the 4-of-4 recipient count is misleading; YubiKey entries are the ONLY ones that survive a host compromise.

**Operator action** — recipe is a temporary imperative override per AGENTS.md, not declarative. After confirming it works, prefer a declarative alternative:
```bash
sudo ln -sf /nix/store/jw2pnlc6syr24biaxmhm7kn6kfp2xm4j-ccid-1.7.1/pcsc/drivers/ifd-ccid.bundle \
            /var/lib/pcsc/drivers/ifd-ccid.bundle
sudo systemctl restart pcscd
```

**Declarative follow-up** (preferred per AGENTS.md "ALL persistent NixOS state lives in `.nix` files under git"):
Add to `hosts/zephyr/configuration.nix` (or a new module):
```nix
systemd.tmpfiles.rules = [
  "L+ /var/lib/pcsc/drivers/ifd-ccid.bundle - - - - /nix/store/jw2pnlc6syr24biaxmhm7kn6kfp2xm4j-ccid-1.7.1/pcsc/drivers/ifd-ccid.bundle"
];
services.pcscd = {
  enable = true;
  # restartTriggers ensure pcscd picks up new driver bundle if/when the ccid pkg regenerates
  restartTriggers = [ "/var/lib/pcsc/drivers/ifd-ccid.bundle" ];
};
```
```

#### F-4. **`modules/system/sops-secrets-registry.nix` declares secrets: {} and never gets populated** ⚠️ LOW (architectural debt)

**Evidence:**
- `sops-secrets-registry.nix:25` declares `secrets = {};` and the `services.sops-secrets-registry.<flag>` toggles (`aiServices`, `kubernetes`, etc.) are defined as options but no other module imports this file with flags set.
- `hosts/{zephyr,nexus,forge,sentry}/configuration.nix` all import `../../modules/system/sops-secrets-registry.nix` (via `common-modules-list.nix`) but **none** set any `services.sops-secrets-registry.<flag>` to `true`.
- Result: Every host's `sops.secrets` attrset is empty.

**Impact:** The actual sops-nix resolution happens via `hosts/<host>/secretspec-creds-wiring.nix` (per-host entry counts: zephyr=22 after this audit's auto-fix #4, nexus=44, forge=8, sentry=12). The `services.sops-secrets-registry.nix` module is dead — it defines options nobody uses, generates empty `secrets = {};`.

**Operator action:** Decide: either declare which secrets **sops-nix** should resolve (and have the registry populate `secrets = { "name" = { ... }; }`), or delete the registry module + remove from `common-modules-list.nix`. The current state is silent dead code, not a deploy break.

#### F-5. **Live Kubernetes manifests use `:latest` images** despite `deny-latest-tag.yaml` ValidatingAdmissionPolicy ⚠️ MEDIUM (severity depends on whether the VAP is enforced — see verification command below)

**Evidence (live manifests, non-archive):**
- `kubernetes-manifests/searxng/03-deployment.yaml:38` — `docker.io/searxng/searxng:latest`
- `kubernetes-manifests/n8n/deployment.yaml:130` — `n8nio/n8n:latest`
- `kubernetes-manifests/amd-device-plugin.yml:31` — `rocm/k8s-device-plugin:latest`
- `kubernetes-manifests/knowledge-fabric/01-deployment.yaml:45` — `localhost/knowledge-fabric:latest` (audit-flagged inline comment; `imagePullPolicy: Never` mitigates runtime risk)
- `k8s/llama-server-deployment.yaml:30` — `image: alpine:latest # Placeholder - we'll override command` (placeholder; not actually deployed but live in the path)

**Impact:** This audit did NOT execute `kubectl get ValidatingAdmissionPolicy deny-latest-tag` from this host. If the policy IS bound and enforcing, the listed manifests would be rejected at admission time (deployments / pod creations would fail). If NOT bound/enforced, the policy itself is dead code and the `:latest` tags are supply-chain hygiene violations without immediate failure. **Either outcome requires action; the framing is "unknown until you verify".**

**Operator action:**
1. `kubectl get ValidatingAdmissionPolicy deny-latest-tag` — confirm policy exists.
2. `kubectl get ValidatingAdmissionPolicyBinding` — confirm policy is bound.
3. For each `:latest` image: pin to a specific version (e.g. `searxng/searxng:2024.12.30-068c793`; `n8nio/n8n:1.94.1`; `rocm/k8s-device-plugin:v1.0.0`).
4. For `localhost/knowledge-fabric:latest`: this is a local-image build with `imagePullPolicy: Never`; pin to commit SHA of upstream knowledge-fabric flake (per inline TODO comment).

#### F-6. **Hardcoded ClusterIPs** in `kubernetes-manifests/knowledge-fabric/01-deployment.yaml` ⚠️ HIGH

**Evidence:** Lines 60–73 hardcode:
- `KF_QDRANT_URL = "http://10.1.1.120:6333"` — Nexus static IP (use `qdrant.ai-inference.svc.cluster.local:6333` instead)
- `KF_LLM_TIER0_URL = "http://10.1.1.140:1235/v1"` — Sentry llama-server port, OK but should use DNS
- `KF_LLM_TIER1_URL = "http://10.1.1.110:1235/v1"` — **zephyr port 1235 is wrong** (actual zephyr llama.cpp is on port **1237** per `kubernetes/modules/llama-servers.nix:295`)
- `KF_LLM_TIER2_URL = "http://10.1.1.110:1234/v1"` — **port 1234 not found** in `kubernetes/modules/llama-servers.nix` (cluster-wide absence not exhaustively verified; see §Verification commands)

`knowledge.md` says "Use DNS names; never hardcode ClusterIPs in NixOS configs". `INFRASTRUCTURE-AUDIT.md` (2026-05-14) confirms this pattern breaks on restart.

**Impact:** `KF_LLM_TIER1_URL` and `KF_LLM_TIER2_URL` will fail at runtime — knowledge-fabric would try to connect to a service that listens on the wrong port. Live and in active deployment path.

**Operator action:**
1. Replace each `http://10.1.1.X:port` with k8s DNS name where possible: `searxng.search.svc.cluster.local:7777`, `qdrant.ai-inference.svc.cluster.local:6333`, `llama-server-sentry.ai-inference.svc.cluster.local:1235`.
2. Fix tier-1: use `http://llama-server-zephyr-3090-moe.ai-inference.svc.cluster.local:1237/v1`.
3. Delete tier-2 (port 1234 not found in llama-servers.nix; verify cluster-wide absence via `nc -zv 10.1.1.110 1234` in §Verification commands before deleting) — set `KF_LLM_TIER2_URL=""`.

#### F-7. **`secrets/default/activepieces.yaml` contains placeholder content** despite recovered from git ⚠️ MEDIUM

**Evidence:** SECURITY-INCIDENT-2026-07-25.md says "the placeholder file lives at the typo'd path for now". The file IS in `secrets/` dir; quick sops-decrypt shows placeholder content (per incident doc).

**Impact:** Cluster has a non-functional `SAMSUNG_TV_TOKEN` (see F-2). SAMSUNG_TV_TOKEN's `secretspec.toml` route points at this file but its content is broken.

**Operator action:** Part of F-2's resolution. Tracked separately for completeness; closing F-2 closes F-7.

#### F-8. **In-tree `agenix` references still present despite migration to sops-nix** ⚠️ LOW (cleanup debt)

**Evidence:** `code_searcher` shows 47 agenix references across:
- `modules/development/ai-coding-tools.nix` (lines 5, 24, 923, 928, 933, 1021, 1024, 1185–1188, 1219) — entire module still says "API keys are read from agenix secrets at runtime"
- `modules/services/mcp-servers.nix:75` comment, `:183`, `:296`, `:408`
- `modules/services/cloudflared.nix:159,165,180` — `requires = ["agenix-rekey.service"]` (broken ref since agenix isn't imported per `modules/default.nix:35`)
- `modules/system/pi-models.nix:10,11,272–278` — systemd `requires = ["agenix.service"]` (broken)
- All 4 host `configuration.nix` files comment # AGENIX secrets (no other action needed; just dead comments)
- `kubernetes-manifests/k8s/opencode/deployment.yaml:13,14,98` — literally `cat /run/agenix/zai-api-key` (wrong path: actual is `/run/secrets/`)

`modules/default.nix:35–36` comments that agenix was "Migrated to sops-nix; agenix module no longer loaded".

**Impact:**
- **F-8a (HIGH):** `cloudflared.nix` and `pi-models.nix` declare `requires = ["agenix.service"]` / `["agenix-rekey.service"]`. These systemd units will fail to start (missing dependency) on any host that enables them. Verify if those modules are currently enabled anywhere via `grep -r 'cloudflared.enable = true\|pi-models.enable = true'` — if yes, deploy breaks.
- **F-8b:** `k8s/opencode/deployment.yaml:98` `cat /run/agenix/zai-api-key` will silently return empty (the path doesn't exist). Already broken; not made worse by ZAI removal.
- **F-8c:** All other comments are documentation drift, no operational impact.

**Operator action:** Decide for F-8a: drop `requires = [agenix...]` from cloudflared.nix and pi-models.nix; OR re-enable agenix if the modules are actually in use. For F-8b: replace `/run/agenix/` path with `/run/secrets/` in the opencode deployment.yaml (and audit all other `/run/agenix/` references).

---

### 🔴 NIXOS DECLARATIVE HYGIENE

#### F-9. **`mkOptionDefault` compliance is mostly clean** ✅ LOW

**Evidence:**
- Pattern search returned 24 `lib.mkOptionDefault` instances in module-level firewall rules. Hosts `zephyr/nexus/sentry/monitoring.nix` use direct assignment (`networking.firewall.allowedTCPPorts = [9100];`) — but these are leaf host-level modules, not shared modules, so direct assignment is correct (no merge risk).
- `modules/services/gitlawb-node.nix:175` uses direct `networking.firewall.allowedTCPPorts = [cfg.httpPort]` — leaf module, OK.
- `modules/services/chatterbox-tts.nix:103` uses direct `networking.firewall.allowedTCPPorts = [8004]` — leaf, OK.

**No action required.** Hosts monitoring.nix patterns are acceptable for leaf-module overrides per AGENTS.md "Use Direct Assignment" table.

#### F-10. **`modules/services/monitoring/ntfy.nix:63` sets `networking.firewall.allowedTCPPorts = []`** ⚠️ LOW

**Evidence:** Line 63 explicitly sets `[]` (empty list). Comment says "Open for localhost only".

**Impact:** If a future contributor adds a port to a shared module via `mkOptionDefault`, this empty-list assignment will OVERWRITE and remove the new port. Marginal safety net. Per AGENTS.md `mkOptionDefault` guidance: this is a leaf-module pattern (ntfy.nix is leaf), so direct assignment is acceptable. **Documented for awareness.**

**No action required** — leave as-is, but consider `lib.mkOptionDefault []` for consistency with other monitoring exports.

---

### 🔴 CODE QUALITY / DEAD CODE

#### F-11. **Dead modules referenced in `modules/default.nix`** ⚠️ LOW (cleanup debt)

**Evidence:**
- `modules/services/image-pullbackoff-debug.nix` — 5-line empty body `{...}: {}` (debug TODO stub).
- `modules/services/hermes-workspace.nix` — only declares `enable` option that prints a deprecation warning. Active deployment is in `kubernetes/modules/hermes-workspace.nix` (K8s wrapper, different namespace).
- Both are imported via `modules/default.nix` and produce EMPTY `config` trees (no actual unit declared).

**Impact:** Trees built; nothing started. Compile-time cost only.

**Operator action:** Decide delete (safe — references are contained) or keep (with explicit `lib.deprecate` or option rename).

#### F-12. **Z.AI cleanup debt — incomplete Z.AI removal** ⚠️ MEDIUM

**Evidence:** 134 `ZAI_API_KEY` references across:
- `modules/development/ai-coding-tools.nix` (134 lines — entire module dead-code-references Z.AI despite `hermes-cli.nix:145-146` "removed 2026-07-15")
- `modules/system/pi-models.nix:5,20,201,234,240,243,257` — `apiKey = "@ZAI_API_KEY@"` template substitution path (operates regardless of whether key is real)
- `modules/development/opencode.nix` — `"apiKey": "{env:ZAI_API_KEY}"` provider config (active if opencode enabled)
- `k8s/opencode/deployment.yaml` — `secretKeyRef: zai-api-key` mounted into opencode container (active path)
- `kubernetes-manifests/ai-inference/` — `zai-api-key` Secret referenced by gateway deployment.yaml multiple times
- `hosts/nexus/services.nix:86,338` — `api_key_env = "ZAI_API_KEY"` and `zaiApiKeyFile = "/run/secrets/zai-api-key"`
- `hosts/zephyr/configuration.nix:668,832` — `apiKeyFile = "/run/secrets/zai-api-key"`
- `kubernetes-manifests/ai-inference/helm/.../templates/deployment.yaml:70` — same ref

This audit removed `ZAI_API_KEY` from `secretspec-creds-wiring.nix` (auto-fix #4) — code reviewer confirmed net effect is unchanged (consumers silently resolve to empty/MISSING_ZAI_KEY today since `ai/zai-api-key.yaml` may not even be encrypted).

**Operator action:** Trace ai-coding-tools.nix, opencode.nix, pi-models.nix end-to-end. Decide per-module:
- (a) Delete Z.AI provider entirely (mirrors hermes-cli.nix:145 cleanup)
- (b) Replace with a different LLM provider (OpenCode Zen, etc.)
- (c) If Z.AI is still intended, restore `ai/zai-api-key.yaml` and re-add the wiring

#### F-13. **Agenix registry references in host config comments** ⚠️ LOW

**Evidence:**
- `hosts/{zephyr,nexus,forge,sentry}/configuration.nix` — each has "# AGENIX secrets — see modules/system/agenix-secrets-registry.nix" comment. The referenced module is now dead per F-8.
- `tests/nixos-eval.nix:28` — "Check that flake inputs include sops-nix (replaced agenix)"

**Operator action:** Replace AGENIX comments with "sops-nix secrets — see modules/system/sops-secrets-registry.nix + secretspec-creds-wiring.nix".

#### F-14. **TODO/FIXME debt** ⚠️ LOW

**Evidence:** `grep TODO|FIXME|XXX|HACK` returns 53 hits across `.nix` and `.md`. Notable:
- `overlay.nix:58` (now removed in this audit)
- `kubernetes/modules/ai-inference.nix:829` "TODO: Fill from sops-nix key"
- `kubernetes/modules/tailscale.nix:159` "TODO: Fill from sops-nix key"
- `kubernetes/modules/mission-control.nix:15` "TODO: when sentry disk is fixed"
- `kubernetes/modules/frostbite-gazette.nix:17,321` (FIXME: image removed)
- `hosts/zephyr/configuration.nix:988,1267` (TODO: option not found; FIXME_CUDA_ON_BUILD)
- `docs/CODE_DEPRECATIONS.md` has tracking table frozen at 2026-04-15 (last update: `CODE-022 @ 2026-05-01`)

**Operator action:** Run `rg -n 'TODO|FIXME' --glob '*.nix' | sort` and triage into a fresh `CODE_DEPRECATIONS.md` row. Mark TIME-BOUND or REMOVE.

---

### 🔴 CLUSTER / ARCHITECTURE

#### F-15. **`kubernetes/modules/host-services.nix:541` — `caddy-local` pinned to zephyr** ⚠️ LOW (documented exception)

**Evidence:** `nodeName = "zephyr"` with comment "Local-only: .ts.net TLS termination requires zephyr certs".

**Impact:** Minor Zephyr OOM exposure for local `.ts.net` reverse-proxying. Acceptable exception per AGENTS.md workload scheduling guidance.

**Action:** Confirm `.ts.net` story is still accurate (Tailscale funnel may have changed). If `.ts.net` is no longer required (e.g., via Tailscale Operator ingress), move caddy-local to nexus.

#### F-16. **`kubernetes/modules/hermes-workspace.nix:31` — `nodeName = "zephyr"` for hermes-workspace pod** ⚠️ MEDIUM

**Evidence:** hermes-workspace is in `workspace` namespace (per namespace labels) and uses Z.AI integration historically (since removed 2026-05-16). Live zephyr OOM exposure as an AI workload.

**Impact:** The module comment says "ARCHIVED (2026-05-16)" but the K8s module is still active and creates the pod every cluster bootstrap. Continued zephyr OOM pressure from an orphaned dev tool.

**Operator action:** Set `replicas = 0` and `selector.matchLabels` no-match to stop scheduling without deleting the manifest (in case restoration is needed); OR move to `nexus` preferred affinity; OR delete the module.

#### F-17. **Active workflow manifests pinned to zephyr/forge by `nodeName` (mining pods)** ⚠️ LOW (acceptable per policy)

**Evidence:** Mining and gaming pods (`kubernetes-manifests/mining/*.yaml`, `csi/*.yaml`) use `nodeName: zephyr|forge|nexus|sentry`. **All** are mining/GPU workloads, which AGENTS.md explicitly allows on zephyr under the OOM policy ("Default to nexus, but mining is acceptable on zephyr given the workload profile").

**No action required.**

#### F-18. **`kubernetes/modules/ai-inference.nix:1819` — secret placeholders with `stringData = { "DATA" = ""; }`** ⚠️ LOW

**Evidence:** Lines ~1290–1380 define K8s Secrets like `ai-inference-gateway-secrets`, `hf-token`, `nvidia-api-key`, `kilo-api-key`, `opencode-api-key`, `opencode-go-api-key`, `ai-gateway-token`. All with empty stringData — populated externally by `kubectl-apply-k8s-secrets` systemd service per `kubernetes/modules/oauth2-proxy.nix:16` warning.

**Impact:** Empty-string K8s secrets would crash pods at startup if `kubectl-apply-k8s-secrets` hasn't run. Verified via comment that it's intentional. No action needed unless that systemd service has drifted.

**Operator action:** Verify `kubectl-apply-k8s-secrets.service` is functional on each cluster host that runs the ai-inference namespace.

#### F-19. **`nodes/templates` contain hardcoded 10.1.1.X IPs everywhere** ⚠️ MEDIUM (knowledge.md calls this out — partially mitigated)

**Evidence:**
- `kubernetes-manifests/calico/network-policies.yaml`, `mining/network-policy.yaml`, `monitoring/prometheus-configmap.yaml`, `security/network-policies-cluster-default-deny.yaml` reference `10.1.1.0/24` and ClusterIP CIDRs `10.0.0.0/8` (mixed correctness)
- `kubernetes/modules/ai-inference.nix` uses `cluster.hosts.<host>.ip` — **good pattern**
- `kubernetes-manifests/knowledge-fabric/01-deployment.yaml` hardcodes 4 IPs — **bad pattern** (see F-6)

`knowledge.md` calls this out: "Use DNS names; never hardcode ClusterIPs in NixOS configs". Many K8s manifests STILL hardcode; easykubenix modules DON'T.

**Operator action:** Migrate remaining `kubernetes-manifests/` path by path to DNS names where possible. Cluster-policy CIDR (`10.0.0.0/8`) hardcoding is **wrong** — k3s service CIDR is `10.43.0.0/16`, not `10.0.0.0/8`. `kubernetes-manifests/calico/coredns-network-policy-fix.yaml:22` shows the CORRECT CIDR (`10.43.0.0/24`) — other policies should match.

#### F-20. **4 live Kubernetes manifests: Pod Security Standards violations** ⚠️ LOW

**Evidence:**
- `kubernetes-manifests/ai-inference/llama-server-deployment.yaml` (or `k8s/llama-server-deployment.yaml:30` if that's the live one) — `securityContext.privileged = true` for GPU access (acceptable per Nvidia-container-runtime caveat in knowledge.md)
- `kubernetes-manifests/ai-inference/gateway-external-service.yaml` — minimal spec, OK
- `kubernetes-manifests/searxng/03-deployment.yaml` — runs as `runAsNonRoot: 1001`, ✅ clean
- `kubernetes-manifests/n8n/deployment.yaml` — postgres + n8n deployments; postgres pod uses `runAsNonRoot: 1000` implicit; n8n pod lacks security context for non-root

**Operator action:** Add explicit `securityContext: {runAsNonRoot: true, runAsUser: 1000, allowPrivilegeEscalation: false, capabilities: { drop: ["ALL"] } }` to n8n pod.

---

### 🔴 DOCUMENTATION DRIFT

#### F-21. **`INFRASTRUCTURE-AUDIT.md` (2026-05-14)** — **NOW MARKED ARCHIVED** ⚠️ LOW (mitigated by auto-fix #5)

The auto-fix replaced the `⚠️ STALE` notice with `⚠️⚠️⚠️ ARCHIVED — DO NOT FOLLOW` plus an explicit drift-markers list. Future readers will not follow its numeric figures.

#### F-22. **`docs/LIVE/STATUS.md` and `docs/LIVE/RUNBOOK.md`** — **NOW FLAGGED STALE** ⚠️ LOW (mitigated by auto-fix #6)

Prominent STALE warnings added. Operator should re-run `just status` / `just docs-audit` and refresh headers when next verified.

#### F-23. **`docs/CODE_DEPRECATIONS.md` last updated 2026-04-15** ⚠️ LOW

**Evidence:** Tracking table rows `CODE-010` through `CODE-022` frozen on 2026-05-01. Many resolved since (NFS purge, Z.AI removal, secretspec migration, hermes-cli cleanup). Need fresh pass.

**Operator action:** Refresh CODE_DEPRECATIONS.md with current state. Mark all resolved items DONE with resolution commit. Add this audit's findings as `AUDIT-2026-07-27-F-001` through `AUDIT-2026-07-27-F-024` (24 items; the report numbers F-1 through F-24) if you want traceability to this report.

#### F-24. **Knowledge.md sections stale on dates** ⚠️ LOW

**Evidence:** `knowledge.md` "Last Updated 2026-07-14" — within acceptable window. But individual sections (architecture patterns, commit-message examples, hard-rule callouts) were verified against the current repo to be MORE accurate than the docs themselves.

**No action required.** knowledge.md is reasonably current.

---

## Resume coverage: 5 audit dimensions

| Dimension | Coverage | Findings |
|-----------|----------|----------|
| Security & secrets | ~95% — covered `secretspec.toml`, `.sops.yaml`, `secretspec-creds.nix`, all 4 host wiring files, env/dotenv posture, k8s Secret placeholders, in-tree references to old systems | F-1, F-2, F-3, F-4, F-7, F-8 |
| NixOS declarative hygiene | ~80% — pattern-searched all module-level firewall rules, host monitoring files; spot-checked a few services | F-9, F-10 |
| Code quality / dead code | ~70% — audited overlay.nix, services/ for dead modules, file_pickered major service configs; TODO/FIXME surfaced via grep | F-11, F-12, F-13, F-14 |
| Cluster / architecture | ~85% — pattern-matched `nodeName` for Zephyr OOM, hardcoded IPs in 103 manifest lines, ValidatingAdmissionPolicy references | F-5, F-6, F-15, F-16, F-17, F-18, F-19, F-20 |
| Doc drift | ~90% — INFRASTRUCTURE-AUDIT, STATUS, RUNBOOK checked explicitly; CODE_DEPRECATIONS flagged | F-21, F-22, F-23, F-24 |

---

## Manual operator actions (priority order)

### THIS WEEK (HIGH severity)

1. **F-1 — `.env` in git history.** Decide rotation, then `git rm --cached .env` and confirm TV still works via Tailscale.
2. **F-2 + F-7 — `SAMSUNG_TV_TOKEN` route + placeholder file.** Add `secrets/default/samsung-tv-token.yaml`, update `secretspec.toml:211`, run `sops updatekeys -y` to align.
3. **F-3 — YubiKey CCID driver symlink on zephyr.** Apply the one-line `ln -sf` recipe to survive zephyr losing both software keys.
4. **F-5 + F-6 — Pin `:latest` images + fix hardcoded IPs in knowledge-fabric.** Verify VAP is in fact enforcing; replace `:latest` with specific versions; replace `http://10.1.1.X:port` with k8s DNS names; fix `KF_LLM_TIER1_URL` port (1235→1237); delete `KF_LLM_TIER2_URL`.

### NEXT WEEK (MEDIUM severity)

5. **F-8a — `cloudflared.nix` and `pi-models.nix` reference dead `agenix.service`.** Decide: drop `requires = [...]` OR re-enable agenix if the modules are actually in use. Critical to verify before any deploy that enables those modules.
6. **F-12 — Z.AI cleanup debt.** Trace 134 `ZAI_API_KEY` refs and decide per-module (delete / swap to OpenCode Zen / restore encrypted key + wiring).
7. **F-8b — `k8s/opencode/deployment.yaml:98` reads `/run/agenix/` not `/run/secrets/`** — already broken, but fix the path to be consistent with the secretspec-creds-wiring write destination.
8. **F-16 — `kubernetes/modules/hermes-workspace.nix` still schedules to zephyr** despite ARchived comment. Either set replicas=0 selectors no-match OR move to nexus.

### WHEN CONVENIENT (LOW severity)

9. **F-4 — `services.sops-secrets-registry.*` options defined but never set.** Decide to populate OR delete the module.
10. **F-11 — Dead debug modules** (`image-pullbackoff-debug.nix`, `hermes-workspace.nix` NixOS side). Delete or deprecate.
11. **F-13 — Replace AGENIX comments.** Dead comments cleaning.
12. **F-14 — TODO/FIXME debt triage.** Refresh `docs/CODE_DEPRECATIONS.md`.
13. **F-19 — Migrate `kubernetes-manifests/` CIDR/policy hardcodes to cluster.hosts DNS.**
14. **F-20 — Add security context to n8n pod.**
15. **F-23 — Refresh `docs/CODE_DEPRECATIONS.md`** (free, after #12). Findings F-1 through F-24 from this audit (`AUDIT-2026-07-27-F-001` through `AUDIT-2026-07-27-F-024`) is the next row to add.

### DROP (acknowledged but not worth the touch)

- **F-9, F-10** — Direct-assignment firewall on leaf modules is acceptable per AGENTS.md, document-only.
- **F-15** — caddy-local exception is documented and intentional.
- **F-17** — Mining pods on zephyr are policy-compliant.
- **F-18** — Empty K8s Secret placeholders are populated externally per design.
- **F-24** — `knowledge.md` is current enough.

---

## Verification commands (re-run before declaring cluster healthy)

```bash
# === Re-verify after operator actions ===

# F-1 + F-2: SAMSUNG_TV_TOKEN + .env tracking
sudo ls -la /etc/nixos/.env  # confirm still on disk
sudo git -C /etc/nixos ls-files --error-unmatch .env 2>&1 | head -3  # confirm not tracked
sudo grep -E 'SAMSUNG_TV_TOKEN' /etc/nixos/secretspec.toml  # confirm updated route
sudo SOPS_AGE_KEY_FILE=/tmp/combined-keys.txt sops -d /etc/nixos/secrets/default/samsung-tv-token.yaml 2>&1 | tail -5  # confirm decrypts

# F-3: YubiKey CCID on zephyr
sudo readlink /var/lib/pcsc/drivers/ifd-ccid.bundle  # confirm symlink
sudo age-plugin-yubikey -l  # confirm both YubiKey recipients listed

# F-5: ValidatingAdmissionPolicy actually enforcing
kubectl get ValidatingAdmissionPolicy deny-latest-tag
kubectl get ValidatingAdmissionPolicyBinding | grep deny

# F-6: hardcoded IPs in knowledge-fabric
kube-inject - < /etc/nixos/kubernetes-manifests/knowledge-fabric/01-deployment.yaml | grep -E '10\.1\.1\.' | head

# F-8a: agenix references that would-block-on-enable
grep -rn '\bagenix\(\|-rekey\)\?\.service\b' /etc/nixos/modules/services/ /etc/nixos/hosts/

# BULK: secretspec validator
SECRETSPEC_SOPS_PROVIDER_BIN=$(nix build .#secretspec-provider-sops --no-link --print-out-paths 2>/dev/null)/bin/secretspec-provider-sops-protocol \
SOPS_AGE_KEY_FILE=/etc/nixos/.age/key.txt \
secretspec check --file /etc/nixos/secretspec.toml --profile production

# syntax check before commit
nix-instantiate --parse /etc/nixos/$(find hosts modules -name '*.nix' -newer /tmp/audit-baseline | tr '\n' ' ')
```

---

## Append-only audit trail

- 2026-03-21 — `DOCUMENTATION_AUDIT_2026-03-21.md` — documentation hygiene
- 2026-05-14 — `INFRASTRUCTURE-AUDIT.md` — cluster state snapshot (now archived)
- 2026-07-25 — `SECURITY-INCIDENT-2026-07-25.md` — SOPS secret data-loss + recovery
- 2026-07-27 — **THIS DOCUMENT** — multi-area audit (security + declarative hygiene + code quality + cluster/architecture + doc drift)

Next audit recommended: after F-1, F-2, F-3, F-5 resolutions, OR after any subsequent FLOS event (secretspec Phase 3, decommission of sops-nix Path B).
