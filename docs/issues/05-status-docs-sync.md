# Issue #5: status doc sync (STATUS / INFRASTRUCTURE-AUDIT / SOPS-NIX / knowledge.md)

**Priority:** LOW (cosmetic; doesn't affect runtime)  \n**Status:** Open  \n**Created:** 2026-07-25  \n**Origin:** Drift-cycle Stream 4b-4e of comprehensive plan  \n**Depends on:** Drift cycle closure (already complete: validate-local exits 0)  \n**Blocks:** nothing; future operator confusion if not addressed

## Context

The drift-cycle Option-B implementation closed the validator-fork resolution gap and made
the cluster's `cluster.localSealSupport` option auto-couple to the sops-registry enable
state via a `?`-guarded `default`. None of the operator-facing status documents were
updated to reflect this land:

- `STATUS.md` is a script-generated snapshot (auto-`scripts/update-status.sh`); the
  auto-refresh footer should add a one-liner because the dragnet history now has a new
  entry.
- `INFRASTRUCTURE-AUDIT.md` is dated **2026-05-14** (71+ days stale as of 2026-07-25);
  the stale-warning footer at the top should at minimum be updated. Auto-generated
  snapshots have a 7-day staleness threshold per the file's own header commentary.
- `SOPS-NIX.md` was updated 2026-07-08 for the hermes bootstrap migration but does NOT
  mention the cluster-wide impure-eval relaxation that came from Option B.
- `knowledge.md` (the AI agent context file) covers the validator chain in its "drift
  cycle disclosure" section but never mentions Option-B's auto-coupling feature.

## Acceptance Criteria

- **`STATUS.md`** — append a `Recent Changes` entry dated `2026-07-25` documenting the
  Option-B materialization + auto-coupling landing (1 line summary). Pattern matches
  existing entries.
- **`INFRASTRUCTURE-AUDIT.md`** — update the staleness-warning line and add a
  `Cluster LocalSealSupport` table row to the audit summary.
- **`SOPS-NIX.md`** — append a final-section note: "Cluster LocalSealSupport — Option B
  landed 2026-07-25: cluster-wide impure-eval relaxation mirrors sops-registry enable
  state; affects only hosts with the local cachix fork checkout (zephyr); other hosts
  remain at upstream cachix behavior."
- **`knowledge.md`** — add a new bullet under "Other services" / "Recent changes" (the
  file has BOTH sections).

## Approach

1. Open each file's most-recent-change section.
2. Append (NOT replace) the relevant paragraph. Style must match surrounding entries.
3. For STATUS.md: don't hand-edit the auto-generated underscore sections; only modify the
   `Recent Changes` block at the bottom (operator narrative). The actual cluster tables
   refresh from `scripts/update-status.sh` (out-of-scope to update manually; let the
   timer do it next hour).
4. For INFRASTRUCTURE-AUDIT.md: update the staleness warning line to acknowledge that the
   file is intentionally a snapshot of audit-time findings + add a footer pointer to the
   drift-cycle plan doc.
5. For SOPS-NIX.md: append (not replace) a `### Cluster LocalSealSupport — 2026-07-25`
   section at the END of the file (after the `Recommended next step (option b): smoke-test
   feature flag — DONE` section).
6. For knowledge.md: append a short bullet under whatever section exists for drift-cycle
   (likely `## Architecture` or `## Gotchas`).

## Risk

- **STATUS.md is auto-generated by `scripts/update-status.sh`**. Hand-edits to the
  per-pod tables will be clobbered on next refresh. ONLY modify the `Recent Changes`
  narrative block.
- **INFRASTRUCTURE-AUDIT.md's staleness warning is itself an operator signal**. Updating
  it requires the broader audit refresh to follow; consider this a tactical commit, not
  a content audit.
- **SOPS-NIX.md is long (800+ lines)**. New sections should go at the END only. Adding
  another `##` header in the middle would break doc-generated TOC links.

## Related

- `STATUS.md` — auto-generated cluster snapshot
- `INFRASTRUCTURE-AUDIT.md` — long-form audit (71+ days stale)
- `SOPS-NIX.md` — sops-nix authoritative reference
- `knowledge.md` — AI agent context file (read by every assistant turn)
- `.plans/2026-07-25-cluster-localSealSupport-scope.md` — the drift-cycle plan doc that
  deserves a backlink from these sync updates
