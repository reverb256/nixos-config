# CI/CD Verification Snapshot — 2026-08-12

> **Status:** Historical verification snapshot
> **Last Verified:** 2026-08-12
> **Source:** GitHub Actions checks and runner API for PR #455
> **Scope:** Commit `9749f80f3875f4dec585d824d0741f2d1c7db588`

## Summary

The self-hosted Nexus runner is working. The normal CI validation jobs passed.
The heavy host-build job did not finish within its three-hour limit, so this PR
must not be described as fully green or deployed.

PR #455 is on branch `issue-454-nixos-sync-safe`. It is not merged to `main` and
has not been deployed to the cluster.

## Observed checks

| Check | Result | Evidence |
|---|---|---|
| Parse Check | Passed | Job `94168474892` |
| Quick Check | Passed | Job `94168474834` |
| Lint Nix | Passed | Job `94168474866` |
| Home Path Guard (#309) | Passed | Job `94168474749` |
| Test Suite | Passed | Job `94169254185` |
| Security Scan | Passed | Job `94168474787` |
| Test Coverage | Passed | Job `94168473953` |
| Build Configs | Cancelled | Job `94169409347`, run `31612907443` |

## Build job detail

`Build Configs` completed these steps successfully:

- Checkout.
- Pre-deploy validation.
- Documentation verification.

The job was cancelled at `2026-08-12T18:36:24Z` after 180 minutes while
`Build all hosts` was still running. Kubernetes manifest validation was skipped.
The cancellation does not prove that any host build completed successfully.

## Runner observation

The GitHub Actions runner API reported `nexus-runner` as `online` and `busy`
when this snapshot was collected. The runner is declaratively configured by
`modules/services/ci-runner.nix` and runs as the `runner` system user on Nexus.

Check live state before relying on this snapshot:

```bash
gh pr checks 455
gh api repos/reverb256/nixos-config/actions/runners
```

## Follow-up

Investigate the host-build duration and per-host logs before treating `Build
Configs` as a reliable merge gate. Do not increase the timeout or bypass the
build job without identifying which host or build phase consumes the time.
