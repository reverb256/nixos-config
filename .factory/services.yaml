commands:
  check: nix flake check
  test: nix flake check
  build: nix build .#nixosConfigurations.zephyr.config.system.build.toplevel
  install: echo "N/A - use just deploy"
  lint: echo "N/A - nix flake check validates options"

services: {}
