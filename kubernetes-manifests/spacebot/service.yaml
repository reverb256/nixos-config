apiVersion: v1
kind: Service
metadata:
  name: spacebot
  namespace: spacebot
  labels:
    app: spacebot
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 19898
      targetPort: 19898
      protocol: TCP
  selector:
    app: spacebot
