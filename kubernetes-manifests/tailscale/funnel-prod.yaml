# Prod Tailscale Funnel Ingresses
# Exposes maplespike services via Tailscale Funnel
# Managed through /etc/nixos/ as source of truth
# Apply: kubectl apply -f funnel-prod.yaml
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: maplespike-api
  namespace: maplespike
  annotations:
    tailscale.com/funnel: "true"
spec:
  ingressClassName: tailscale
  rules:
  - http:
      paths:
      - backend:
          service:
            name: maplespike-api
            port:
              number: 8082
        path: /
        pathType: Prefix
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: maplespike-portal
  namespace: maplespike
  annotations:
    tailscale.com/funnel: "true"
spec:
  ingressClassName: tailscale
  rules:
  - http:
      paths:
      - backend:
          service:
            name: maplespike-portal
            port:
              number: 8080
        path: /
        pathType: Prefix
