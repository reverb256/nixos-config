apiVersion: v1
kind: Service
metadata:
  name: searxng-nodeport
  namespace: search
  labels:
    app: searxng
spec:
  type: ClusterIP  # DISABLED: NodePort removed (2026-03-23) - Using Caddy ingress
  selector:
    app: searxng
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
