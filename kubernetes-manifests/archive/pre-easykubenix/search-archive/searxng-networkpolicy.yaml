---
# NetworkPolicy to allow ingress to SearXNG - Allow all traffic for now
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-searxng-ingress
  namespace: search
spec:
  podSelector:
    matchLabels:
      app: searxng
  policyTypes:
  - Ingress
  ingress: []
