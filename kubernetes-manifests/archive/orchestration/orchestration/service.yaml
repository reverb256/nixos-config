apiVersion: v1
kind: Service
metadata:
  name: mission-control
  namespace: orchestration
  labels:
    app: mission-control
spec:
  type: ClusterIP
  selector:
    app: mission-control
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
