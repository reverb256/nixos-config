apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: spacebot
  namespace: spacebot
  labels:
    app: spacebot
spec:
  selector:
    matchLabels:
      app: spacebot
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
