# XMRig CPU Miner - Sentry (16 cores, 4 threads × 2 replicas = 50%)
# - privileged for MSR register access
# - 2 replicas at 25% each = 50% CPU cap
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: xmrig-sentry
  namespace: mining
  labels:
    app: xmrig-sentry
    host: sentry
    workload: crypto-mining
spec:
  replicas: 2
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: xmrig-sentry
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: xmrig-sentry
        host: sentry
        workload: crypto-mining
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8082"
        prometheus.io/path: "/1/summary"
    spec:
      nodeName: sentry
      serviceAccountName: gpu-miner-sa
      automountServiceAccountToken: false
      priorityClassName: mining-low
      hostNetwork: true
      hostIPC: true
      hostPID: true
      dnsPolicy: ClusterFirstWithHostNet
      tolerations:
        - key: node-role.kubernetes.io/control-plane
          operator: Exists
          effect: NoSchedule
      containers:
      - name: xmrig
        image: docker.io/library/xmrig-alpine:6.25.0
        imagePullPolicy: Never
        args:
          - "-o"
          - "10.1.1.120:3333"
          - "-u"
          - "sentry-cpu"
          - "--tls=false"
          - "--threads=4"
          - "--donate-level=1"
          - "--http-enabled"
          - "--http-host=0.0.0.0"
          - "--http-port=8082"
        ports:
        - containerPort: 8082
          name: api
          protocol: TCP
        resources:
          requests:
            memory: "1Gi"
            cpu: "2"
          limits:
            memory: "3Gi"
            cpu: "4"
        securityContext:
          privileged: true
        volumeMounts:
        - name: msr
          mountPath: /dev/cpu
          mountPropagation: HostToContainer
        - name: hugepages
          mountPath: /dev/hugepages
          mountPropagation: HostToContainer
        - name: sys-module-msr
          mountPath: /sys/module/msr
          mountPropagation: HostToContainer
        - name: tmp
          mountPath: /tmp
        livenessProbe:
          httpGet:
            path: /1/summary
            port: 8082
          initialDelaySeconds: 30
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /1/summary
            port: 8082
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
      volumes:
      - name: msr
        hostPath:
          path: /dev/cpu
          type: Directory
      - name: hugepages
        hostPath:
          path: /dev/hugepages
          type: Directory
      - name: sys-module-msr
        hostPath:
          path: /sys/module/msr
          type: Directory
      - name: tmp
        emptyDir: {}
