# XMRig Variable CPU Miner - Sentry (4 additional threads, scales to 0 during builds)
# Combined with xmrig-sentry (4 threads) = 8/16 threads (50%)
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: xmrig-sentry-variable
  namespace: mining
  labels:
    app: xmrig-sentry-variable
    host: sentry
    workload: crypto-mining
    type: variable
spec:
  replicas: 1
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: xmrig-sentry-variable
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: xmrig-sentry-variable
        host: sentry
        workload: crypto-mining
        type: variable
    spec:
      nodeName: sentry
      serviceAccountName: gpu-miner-sa
      automountServiceAccountToken: false
      priorityClassName: preemptible-mining
      hostNetwork: true
      hostIPC: true
      hostPID: true
      dnsPolicy: ClusterFirstWithHostNet
      tolerations:
        - key: node-role.kubernetes.io/control-plane
          operator: Exists
          effect: NoSchedule
      containers:
      - name: xmrig
        image: docker.io/library/xmrig-alpine:6.25.0
        imagePullPolicy: Never
        args:
          - "-o"
          - "10.1.1.120:3333"
          - "-u"
          - "sentry-cpu-var"
          - "--tls=false"
          - "--threads=4"
          - "--donate-level=1"
          - "--http-enabled"
          - "--http-host=0.0.0.0"
          - "--http-port=8083"
        resources:
          requests:
            memory: "1Gi"
            cpu: "2"
          limits:
            memory: "3Gi"
            cpu: "4"
        securityContext:
          privileged: true
        volumeMounts:
        - name: msr
          mountPath: /dev/cpu
          mountPropagation: HostToContainer
        - name: hugepages
          mountPath: /dev/hugepages
          mountPropagation: HostToContainer
        - name: sys-module-msr
          mountPath: /sys/module/msr
          mountPropagation: HostToContainer
        - name: tmp
          mountPath: /tmp
      volumes:
      - name: msr
        hostPath:
          path: /dev/cpu
          type: Directory
      - name: hugepages
        hostPath:
          path: /dev/hugepages
          type: Directory
      - name: sys-module-msr
        hostPath:
          path: /sys/module/msr
          type: Directory
      - name: tmp
        emptyDir: {}
