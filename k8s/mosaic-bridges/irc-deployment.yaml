---
apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: "mosaic-bridge-irc"
  namespace: "orchestration"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "mosaic-bridge-irc"
  template:
    metadata:
      labels:
        app: "mosaic-bridge-irc"
    spec:
      initContainers: []

      containers:
        -
          name: "bridge"
          image: "nexus:5000/mosaic-bridges:v0.1.0"
          imagePullPolicy: "IfNotPresent"
          env:
            -
              name: "MIS_URL"
              value: "http://mosaic-identity:8081"
            -
              name: "BRIDGE_TYPE"
              value: "irc"
            -
              name: "IRC_PORT"
              value: "6697"
            -
              name: "IRC_SERVER"
              value: "irc.libera.chat"
          securityContext:
            runAsUser: 100
            runAsGroup: 101
            runAsNonRoot: true
          resources:
            requests:
              cpu: "50m"
              memory: "32Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
