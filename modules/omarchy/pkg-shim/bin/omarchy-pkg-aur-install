#!/bin/bash

# omarchy:summary=Show a fuzzy-finder TUI for picking AUR packages (not available on NixOS).

echo "Error: the AUR is not available on NixOS." >&2
echo "Use \`omarchy pkg install\` to browse nixpkgs instead." >&2
exit 1
