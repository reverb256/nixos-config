@@ -73,3 +73,5 @@
-  # OpenClaw AI Gateway Service - running as system service (lobster user)
-  # Authentication is now MANDATORY (removed --allow-unconfigured flag)
+  services.openclaw = {
+    enable = true;
+    port = 18789;
+    host = "loopback";
+    autoStart = true;
+    extraArgs = [];
+  };
