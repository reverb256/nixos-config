# ── Nix ───────────────────────────────────────────────────────────
result

# ── Secrets and keys ──────────────────────────────────────────────
# ✅ DO commit: .age files are encrypted and safe to track
# ✅ DO commit: secrets.nix contains only public keys
# ❌ NEVER commit: decrypted files or private keys
.age/
secrets/*.key
secrets/*.plaintext
secrets/*.secret
secrets/*.txt

# ── Agent artifacts ───────────────────────────────────────────────
.agents/
.bug-hunter/
.claude/
.hermes/
.hive/
.pi/
.pi-lens/
.pi/secrets.yaml
.serena/
.sisyphus/
skills/
skills-lock.json

# ── Git worktrees ─────────────────────────────────────────────────
.worktrees/

# ── Editor backups + patch-rejection artifacts (#277, 2026-07-08) ──
*.bak*
*.swp
*.orig
*.rej
*~

# ── Build artifacts ───────────────────────────────────────────────
__pycache__/
sbom/
node_modules/
package.json
package-lock.json
.pnpm-debug.log*
.yarn/*
.yarn-integrity

# ── Scratch / debug / temporary files ──────────────────────────────
.scratch/
tmp/
tmpfile
cat
echo
hello_test
usb.qcow2
Updated API key file

# ── Vendor / third-party ──────────────────────────────────────────
istio-1.29.1/
vllm-env/
void-config/
void-mklive/

# ── Miscellaneous ─────────────────────────────────────────────────
.diff-scores.json
.firecrawl/
.pipeline-guard-disable
/kubernetes/modules/monitoring.nix.bak
lucebox-hub
lucebox-hub.git/
modules/services/secrets/kubernetes/encryption-config.yaml
ssh/
territory_map.json
hey.md

# ── Emergency cleanup additions (2026-03-19) ───────────────────────
# (moved into organized sections above)

# ── Security cleanup (2026-03-30) ─────────────────────────────────
# (moved into organized sections above)
