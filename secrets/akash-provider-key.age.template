# Akash Provider Wallet Key
# This file contains the private key for your Akash provider wallet
# Store the key.pem file contents here (base64 encoded or raw PEM format)
#
# HOW TO CREATE YOUR WALLET:
#
# Option 1: Using Akash Console (Web)
# 1. Go to https://console.akash.network
# 2. Connect/Create wallet (Keplr or Leap)
# 3. Copy your wallet address for providerAddress in NixOS config
# 4. Export private key from wallet (see wallet's export feature)
# 5. Paste the key content below
#
# Option 2: Using CLI (when available)
# 1. akash keys add cluster-provider
# 2. SAVE THE MNEMONIC SECURELY!
# 3. akash keys export cluster-provider --unarmored-hex --unsafe
# 4. Paste output below
#
# SECURITY NOTES:
# - Never commit this file to git
# - Keep a backup of your mnemonic in a secure location
# - If you lose this key, you lose access to your provider earnings
# - Consider using a separate wallet for provider operations

# Paste your key content below (either base64 encoded or PEM format):
