# OneTool Configuration
version: 2

# Glob patterns for custom tool discovery
tools_dir: []

# Tool-specific configuration
tools:
  context7:
    api_key_file: /run/agenix/context7-api-key
  ground:
    secrets:
      gemini:
        api_key_file: /run/agenix/gemini-api-key
