# Distributed Build Machines File
# Generated for Nexus as primary coordinator
# Copy to ~/.config/nix/machines on the coordinator host

nexus@nexus {
  hostName = "10.1.1.120";
  system = "x86_64-linux";
  maxJobs = 12;
  speedFactor = 10;
  supportedFeatures = ["kvm" "benchmark" "big-parallel"];
  mandatoryFeatures = [];
}

zephyr@zephyr {
  hostName = "10.1.1.110";
  system = "x86_64-linux";
  maxJobs = 0;
  speedFactor = 1;
  supportedFeatures = ["kvm" "benchmark"];
  mandatoryFeatures = [];
}

forge@forge {
  hostName = "10.1.1.130";
  system = "x86_64-linux";
  maxJobs = 4;
  speedFactor = 3;
  supportedFeatures = ["kvm" "benchmark" "big-parallel"];
  mandatoryFeatures = [];
}

sentry@sentry {
  hostName = "10.1.1.140";
  system = "x86_64-linux";
  maxJobs = 8;
  speedFactor = 5;
  supportedFeatures = ["kvm" "benchmark" "big-parallel"];
  mandatoryFeatures = [];
}